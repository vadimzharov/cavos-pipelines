#cloud-config

stylus:
  site:
    edgeHostToken: ${EDGE_HOST_TOKEN}

install:
  poweroff: true

stages:
  initramfs:
  - name: "Core system setup"
    users:
      spectro:
        groups:
          - admin
        passwd: vadim